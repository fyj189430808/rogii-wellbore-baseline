# Task 1 独立二次复核

## 一、明确结论

- **规格符合性：通过。** 原复核提出的 5 项重要问题和 5 项轻微问题均已实质关闭；固定单模、v2 S/C 顺序、统一门槛、防泄漏、缓存血缘、artifact、Typewell 压力测试、RF01 两层结论及 Git 限制保持完整。
- **文档质量：批准。** 两处原有规则冲突已经统一，关键选择和停止控制流现在可以直接执行；中文解释、字段合同和完成标准也足够明确。
- **未关闭问题：无。** 本轮未发现新的严重、重要或轻微问题。

## 二、复核范围与方法

本轮重新完整读取：

1. 更新后的 `H:\kaggle\716\AGENTS.md`
2. 更新后的 `H:\kaggle\716\.superpowers\sdd\task-1-report.md`
3. 原 `H:\kaggle\716\.superpowers\sdd\task-1-review.md`

随后逐项复核原报告中的 5 项重要问题和 5 项轻微问题，并独立检查当前文件的章节、围栏、行数、SHA256 及关键冲突句。只使用只读命令；未使用 Git，未运行实验，未修改项目文件。本文件是本轮唯一覆盖写入的复核产物。

## 三、五项重要问题关闭情况

### 1. 隐藏真实 TVT 与 oracle 用途冲突：已关闭

- `AGENTS.md:119` 将自然隐藏真实 TVT 的用途统一为最终评分或完全隔离的 oracle 诊断。
- `AGENTS.md:286` 进一步要求 oracle 与正式特征、缓存和候选选择完全隔离，并禁止其反哺模型、公式、窗口、阈值或晋级决定。
- 原来“只在最终评分时读取”的绝对句已不存在，两处规则现在一致。

### 2. 停止当前实现与暂停整条路线冲突：已关闭

- `AGENTS.md:11` 已明确正常路线图停止条件只停止当前实现，留档后继续下一项。
- `AGENTS.md:161-176` 将停止条件明确分为“正常失败”和“合同异常”：前者继续路线，后者暂停整条路线并先修复。
- `AGENTS.md:438-439` 在长任务规则中再次使用相同分类，没有相反指令。

### 3. C 系列候选资格和数量不清：已关闭

- `AGENTS.md:62` 明确 C 候选池只包含完整五折且满足第 3.3 节全部门槛的 S 组，未晋级组不得补位。
- `AGENTS.md:74` 明确 0/1/2/3 个晋级组分别不运行 C、只运行 C01、运行到 C02、运行到 C03。
- `AGENTS.md:508` 在完成标准中再次限定 C 系列只对完整五折晋级组运行。

### 4. PF/Beam 单井 cache 缺少 `well_id`：已关闭

- `AGENTS.md:328-339` 要求每个单井 PF/Beam cache 在 `cache_manifest.json` 中单独记录 `well_id`、`outer_fold`、`fit_wells_hash`、`pf_config_hash`、`seed_list_hash`、`prediction_row_hash`。
- 文档还明确通用井列表不能替代单井产物自己的 `well_id`，消除了归属歧义。

### 5. `feature_quality.csv` 缺少 `feature`：已关闭

- `AGENTS.md:380-395` 明确质量表每行对应一个特征，第一列必须是 `feature`。
- v2 要求的 13 个最低字段及顺序已完整列出。

## 四、五项轻微问题关闭情况

### 1. C 系列缺少不同信息源原则：已关闭

`AGENTS.md:75-77` 已同时规定五折改善排序、预先登记近似同分范围和同分规则，并明确优先保留不同信息源、避免高度同源堆叠；规则必须在看到条件实验结果前冻结。

### 2. fold 0 错引完整统一门槛：已关闭

`AGENTS.md:92` 只在 fold 0 判断 0.15 ft 子门槛，并明确完整五折、4/5 折、bootstrap 和 P90 此时尚不能判断；原歧义句已删除。

### 3. “全部适用 S 系列”范围过宽：已关闭

`AGENTS.md:54` 和 `AGENTS.md:507` 将 F05b 定义为唯一预定取消例外，要求先保存 F05a 的完整证据和取消决定，且明确不能类推到其他 S 实验或用“暂不适用”跳过。

这里的“F05a 完整验证”结合第 2.3 节分阶段停止规则，指按预定流程运行到其合法终点；不会要求已经在 fold 0 命中停止条件的实现违规补跑五折。

### 4. 首次英文术语缺少中文解释：已关闭

micro RMSE、bootstrap、oracle、private-safe、branch-aware self-template、horizon-matched prefix holdout、stacking、OOF 和 Typewell fingerprint stress test 均已在首次实质使用处配有普通中文解释；outer fold 也在固定验证规则首次承担操作含义时解释为“外层验证折”。路线图固定实验名仍保留英文，不影响理解。

### 5. 原报告仅做关键词存在检查：已关闭

更新后的实施报告新增“复核修正”章节，逐项记录必需语义、冲突旧句不存在、字段连续顺序以及结构检查。本复核没有仅采信报告，而是重新读取 `AGENTS.md` 并逐项核验，上述语义均实际存在。

## 五、整体合同复核

- 12 个顶层章节名称和顺序正确，编号为 0–11。
- B0、F01、F02a、F02b、F03a、F03b、F04、F05a、F05b、F06、F07R 的 S 系列顺序与 v2 一致。
- 单个 LightGBM、固定参数、1,734 棵树、seed 29、`spatial_pad_1000_v1`、固定 target、训练范围、评价行和评分代码均未放松。
- 八项晋级门槛、井级聚类 bootstrap 口径、等维负对照、隐藏真值不变性、B0 hash、缓存完全匹配、完整 artifact 文件及固定 leakage 检查名均保留。
- F03/F04 的 primary/strict Typewell 指纹压力测试仍明确不替代主 CV。
- 原 RF01 八版本仍为“不晋级”；`RF01_stability_audit_v1` 仍固定 RF01a、复用 folds 0–1、只补 folds 2–4，不能重选或追认。
- PF 10.7 复现和导读文档没有重新成为实验门槛；未发现旧路线图路径或旧研究禁令。
- Git、worktree、分支、提交、推送和 PR 仍只允许在用户单独明确提出后执行。

## 六、独立验证结果

当前 `AGENTS.md`：

```text
行数：521
Markdown 围栏线：30
顶层章节：12/12，顺序正确
SHA256：03AB662AD8FBF25A8E48F45796138840565A38B3A11145B7A28B7018A481A1C9
```

这些结果与更新后的实施报告一致。

## 七、静态复核边界

- 未使用 Git，也没有修改前快照，因此不能独立证明实施过程中没有触碰其他文件或执行过 Git；只能确认本复核过程没有这样做，且实施报告作了相应声明。
- 未运行实验，无法从规则文本证明既有 B0、PF/Beam cache、RF01 预测或未来 artifact 的实际合法性与可复现性。
- 本结论批准的是 `AGENTS.md` 作为后续实验执行合同的规格与文字质量，不代表路线图实验本身已经完成。
