# P2-F01 连续 GR offset 路径执行计划

1. 为二阶状态动态规划和支持度观测代价先写合成单元测试。
2. 实现纯合法核心：得分张量转观测代价、二阶前向/后向求解、逐行插值。
3. 加入三类程序级正对照：合成路径、可见前缀 12 ft 半正弦注入、全 GR 缺失回零。
4. 实现逐井运行器；第一次读取只允许 `MD/GR/TVT_input`、Typewell `TVT/GR` 和冻结 PF 缓存。
5. 运行 3 井 smoke，检查行键、合法缓存列、程序对照和可恢复写入。
6. 技术检查通过后运行 773 井；合法路径全部落盘后再独立读取隐藏 TVT 评分。
7. 对照实验卡汇总原 PF、连续路径、循环平移和逐块独立路径；不事后调状态或门槛。
8. 若诊断通过，登记独立的正式特征实验；若失败，写清只能否定的具体实现，再进入路线图下一项。

