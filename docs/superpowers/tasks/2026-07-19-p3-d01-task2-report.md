# P3-D01 Task 2 实现报告

## RED / GREEN

新增 Task 2 测试后，先运行：

```powershell
python -m pytest rogii_clean/tests/test_p3_d01_pf_observation_audit.py -q --basetemp .pytest_tmp_p3_d01_task2
```

首次 RED 是收集期 `ImportError`：

```text
cannot import name 'read_likelihood_cache' from 'src.p3_d01_pf_observation_audit'
```

这是新规定接口尚未实现导致的预期失败。完成最小实现后，以同一命令复验，GREEN 为：`29 passed in 6.66s`。

## 修改文件

- `rogii_clean/tests/test_p3_d01_pf_observation_audit.py`
- `rogii_clean/src/p3_d01_pf_observation_audit.py`
- `docs/superpowers/tasks/2026-07-19-p3-d01-task2-report.md`

## 实现与自审

- 单井审计直接复用 `prepare_particle_filter_inputs`、`_kernel_arguments`、`particle_filter_all_seeds_numba` 与 `_likelihood_weighted_path`；没有重写随机内核或加权矩阵计算。
- `path_features` 只在内存返回，严格包含指定的 8 个键；均值和四个尺度路径按 P2-P01 的 float32 转换及减去 `last_visible_tvt` 的顺序计算。
- 报告合并 Task 1 的合法井级 GR 指标、可见前缀诊断、种子对数似然汇总，以及尺度 3/5/8/12 的 ESS、熵和 top-4 mass 指标；不读取隐藏 `TVT`、surface、target、error、RMSE 或 oracle。
- NPZ 仅写入 `seed_log_likelihoods` 与 `cache_fingerprint`。写入通过已打开的 `.tmp` 二进制文件并以 replace 原子替换；读写均校验一维、非空、有限值、指纹和种子数。测试覆盖 round-trip、元数据不匹配、无效值、损坏归档及模拟写入失败。
- smoke 测试采用 8 粒子、3 seeds、5 隐藏行；未运行 657 口开发井，未修改 runner、配置、旧 PF、状态文档或 Git。

## 风险

- 当前仅有合成单井 smoke 覆盖；真实 657 口开发井的耗时、缓存中断恢复与汇总输出仍需后续 runner 任务验证。
- 缓存原子语义依赖同一目录内的 `replace`；写失败时临时文件会清理，既有最终文件若存在则保持不变。
