# P3-D01 Task 4B：runner 接入开发目标、分析产物和路线判断

## 前置

等待 Task 3 的 legal runner 与 Task 4A 的纯统计模块都通过审查后再实现。只允许修改：

- `rogii_clean/scripts/diagnose_p3_d01_pf_observation_weights.py`
- `rogii_clean/tests/test_diagnose_p3_d01_pf_observation_weights.py`
- 如配置尚缺只读评分来源，可补 `rogii_clean/configs/p3_d01_pf_observation_weight_audit_v1.json`

不要修改旧 PF、Task 1/2 核心或 Task 4A 统计公式。

## TDD

先补以下失败测试并运行 RED：

1. `load_development_targets` 从同时含开发/影子井的临时 Parquet 加载，只返回 selected 开发井；极端 shadow target 不得出现在返回表。必须通过 Arrow filter 在扫描阶段排除，而不是整表读入后删除。
2. `score_development_paths` 用两口小井、乱序 `row_index` 和临时 P1 legal cache，结果与 Task 4A 手算一致；返回表一井一行。
3. legal generation 返回任一 error 时，主流程必须在调用 target loader 之前失败。
4. legal per-well 文件不能含 `target/error/rmse/oracle` 字样；评分文件必须保存 `best_scale_is_oracle=True`。
5. smoke/fold01/all 的输出名互不覆盖；all 另写无后缀的最终合同文件。

## 数据隔离顺序

主流程必须保持：

```text
读取 fold 和 shadow 井号
→ 创建不含 shadow 的 selected tasks
→ 完成并验证所有 selected legal caches
→ errors 必须为空
→ 保存只含合法统计的 legal/per_well_<mode>.csv
→ 这时才从 P2-P02 predictions 读取 selected 开发 target
→ 连接旧 P2-P01 合法路径并评分
```

目标列固定：

```text
well_id, fold, row_index, target_tvt, pred_tvt
```

Arrow filter 必须同时限制 selected well IDs，并再次排除 116 个 shadow IDs。返回后检查井数、行数、键唯一、有限性、每井单 fold 和 shadow overlap=0。

## 评分与分析

每井读取旧 P2-P01：

```text
artifacts/P2_P01_multiseed_pf_mean_v1/legal_cache/<well_id>.parquet
```

调用 Task 4A：

- `score_well_paths`
- `aggregate_path_metrics`
- `add_diagnostic_columns`
- `correlation_tables`
- `build_binned_metrics`
- `decide_pf_routes`

固定相关 pairs：

```text
missing_gr_fraction              ↔ pf128_mean_rmse
longest_gr_gap_md_ft             ↔ pf128_mean_rmse
scale_{3,5,8,12}_effective_sample_size ↔ hidden_rows
scale_{3,5,8,12}_effective_sample_size ↔ observed_gr_fraction
scale_{3,5,8,12}_effective_sample_size ↔ pf128_mean_rmse
scale_{3,5,8,12}_effective_sample_size ↔ p2p02_rmse
```

置换次数固定 1000，seed 固定写入 config。相关只做井级诊断，不写出沿 MD 的结论。

## 输出

模式阶段文件带 `_smoke` / `_fold01` / `_all`；完整 all 还复制为最终合同名：

```text
legal/per_well_<mode>.csv
oracle/per_well_error_join_<mode>.csv
per_fold_correlations_<mode>.csv
overall_correlations_<mode>.csv
binned_metrics_<mode>.csv
scale_path_metrics_<mode>.csv
summary_<mode>.json
runtime_<mode>.json
conclusion_<mode>.md
```

all 最终合同：

```text
per_well.csv
per_fold_correlations.csv
overall_correlations.csv
binned_metrics.csv
scale_path_metrics.csv
summary.json
runtime.json
conclusion.md
```

`per_well.csv` 可含合法统计和逐井错误，但必须放在分析根目录，不得放入 `legal/`；为避免误解，建议与 `oracle/per_well_error_join_all.csv` 内容一致并明确 `contains_target_derived_metrics=true`。

不写逐行 target/prediction，不写 model、feature_list 或正式 predictions。

## summary 和结论

summary 至少保存：

- 固定血缘 `P3B00 full 773 micro=10.305704992073148`
- 当前模式井数/行数、shadow overlap=0
- P2-P02、PF mean、四固定温度和 oracle temperature 的 pooled RMSE
- 最佳全局固定温度与 oracle 的差值
- PF01/PF02 每个子条件证据和最终支持判断
- 明确 `model_training=false`、`formal_feature_output=false`、`shadow_target_access=false`

结论按 AGENTS 模板分：事实、合理推断、未验证猜测、当前只能否定、下一步最便宜验证。下一步编号必须遵照原三阶段路线：PF01=缺失感知稳健 GR 标定，PF02=固定有效路径数，PF03=沿 MD 分段/动态似然，R01=严格 OOF 低维残差；D01 不能把 whole-well LL 说成已验证 PF03。

## 完成条件

- 新增测试先 RED 后 GREEN；
- 聚焦测试与已有 Phase3 回归测试通过；
- 不运行真实 fold01/all，由主代理在审查后运行；
- 不做 Git。
