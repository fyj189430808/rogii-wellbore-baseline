# ROGII 项目协作规则（二阶段持续实验版）

## 1. 当前唯一目标

当前阶段在一阶段最佳方案 C01 的基础上继续探索有效数据表示和特征，尽量降低可信的自然隐藏行 pooled micro RMSE。

不再把“彻底看懂并复现 PF 10.7”作为前置门槛，也不回到一阶段空间 CV 继续筛特征。

## 2. 二阶段冻结合同

- 主 CV：`balanced_well_5fold_v1`
- 验证单位：完整井，同一口井不能同时出现在训练和验证
- 二阶段基线：`P2_CV00_group5_c01_v1`，简称 `P2B00`
- 基线 micro RMSE：`12.539227823724785`
- 评价行：`TVT_input.isna()`，共 3,783,989 行、773 口井
- target：`TVT - last_visible_tvt`
- 模型：始终只有一个 `lightgbm.LGBMRegressor`
- 参数：固定 `lgbm_feature_baseline_v1`，1,734 棵树，seed 29，无 early stopping
- 输出：`last_visible_tvt + predicted_delta`
- 行权重、评分代码和评价范围固定
- 不做模型融合、stacking、第二学习模型或学习型输出后处理
- 不使用测试期不可获得的信息

一阶段 `spatial_pad_1000_v1` 上的 `12.930691493610633` 只保留为旧协议历史成绩，不与二阶段分数直接判优劣。

## 3. 当前允许改变的内容

每次实验只允许改变一个命名清楚的特征组或一个确定性路径表示。

允许：

- 从当前井 `MD/X/Y/Z/GR/TVT_input` 构造测试期合法特征
- 使用对应 Typewell 的 `TVT/GR`
- 使用冻结 PF、Beam、C01 候选路径及其确定性变换
- 构造多尺度 GR 得分面、连续 offset 路径和前向—后向平滑路径，作为 LightGBM 输入特征
- 在 outer fold 内只用训练井拟合空间地层先验，再预测验证井
- 使用 Typewell marker 或模板构造归一化坐标，前提是测试时可获得或能由合法模板匹配得到
- 使用隐藏 TVT 做明确标注的 oracle/诊断，但不得把诊断真值进入正式特征

不允许：

- 调 LightGBM 参数或树数
- 改 target、评价行或主 CV
- 把新路径直接与模型输出融合以绕过单模限制
- 使用验证井隐藏 TVT、验证井 surface 或任何标签派生量构造正式输入
- 在 outer fold 外拟合邻井、surface、标准化、阈值或模型选择
- 偷偷复用一阶段折号或旧模型产物

## 4. 执行顺序

二阶段路线以 `rogii_clean/experiments/phase2_roadmap.md` 为准：

```text
P2-CV00  按井五折冻结基线（已完成）
P2-D01   PF 中心多尺度 GR 可辨识性审计（已完成，固定局部打分未通过）
P2-S01   严格 outer-fold 空间地层路径
P2-F01   在更强结构中心上做连续 offset 路径与前向—后向平滑特征
P2-M01   marker 归一化坐标路径
```

若某项产生明确新证据，可以在路线图中登记一个最小后续版本；不得同时改多个信息源。

## 5. 自动连续执行

用户没有说停止时，Codex 应连续推进：

```text
读取 current_state
→ 写最小实验卡
→ 实现或复用特征缓存
→ 1～3 井 smoke
→ 诊断或 folds 0–1
→ 按预设门槛自动停止或完成五折
→ 保存结论
→ 更新 current_state 和 registry
→ 进入下一项
```

普通实验不请求重复确认。只有以下情况才暂停：

- 需要改变冻结合同
- 需要测试期不可用信息
- 出现无法自动判断的数据缺失或歧义
- 用户明确要求停下

Git、提交、推送和 PR 不是实验主线。除非用户明确要求 Codex 操作，否则只在阶段节点给用户可执行命令，不因 Git 或 worktree 中断实验。

## 6. 晋级门槛

正式特征实验相对当前二阶段最佳方案比较：

- folds 0–1 合并改善至少 `0.20 ft`
- folds 0、1 中任一折不得恶化超过 `0.25 ft`
- 通过后自动跑完整五折
- 完整五折 micro RMSE 改善至少 `0.10 ft`
- 至少 `4/5` 折同方向
- 井级配对 bootstrap 95% CI 上界小于 0
- P90 well RMSE 不恶化超过 `0.5 ft`
- 收益不能只来自极少数井

诊断实验不按模型分数晋级，而按实验卡中预先冻结的可辨识性指标决定是否进入路径实现。

负结果只能否定当前实现，不能擅自宣布整个信息源无效。

## 7. 防泄漏和缓存

- 所有 outer-train 拟合量必须在每个 fold 内重算
- 验证井不得参与空间曲面、邻井统计、阈值或标准化拟合
- 缓存指纹必须包含 fold registry、井列表、特征版本、参数和生成代码
- 从一阶段复用的逐井确定性数值缓存可以保留，但必须按 `well_id` 覆盖旧 fold，并记录旧 fold 只属于来源血缘
- 隐藏 TVT 删除或修改后，正式特征必须不变
- oracle 输出必须单独保存，不能混入正式 feature cache

## 8. 运行和记录

长任务开始前说明井数、主要耗时、输出目录和恢复方式；运行中至少在 smoke、每折完成、停止门槛或异常时报告。

实验结果保存到独立目录：

```text
rogii_clean/artifacts/<experiment_id>/
```

至少记录：配置、完整特征列表、参数、逐折结果、运行时间和简明结论。完整五折候选还要保存完整 OOF、逐井指标、bootstrap、来源和泄漏审计。

当前状态写入：

```text
rogii_clean/experiments/current_state.json
```

正负结果都追加到：

```text
rogii_clean/experiments/registry.jsonl
```

## 9. 结论写法

每项实验至少区分：

```text
事实：
推断：
仍未验证：
当前只能否定：
下一步：
```

所有说明尽量使用清晰中文。不要用长篇流程文字拖慢简单实验，也不要用术语替代证据。
