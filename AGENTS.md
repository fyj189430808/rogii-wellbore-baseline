# ROGII 项目协作规则（Phase 4 双轨实验版）

## 1. 当前阶段目标

Phase 4 使用双轨合同：

1. 特征研究轨道继续固定单个 LightGBM、固定 CV、固定参数，只判断新特征是否有增量；
2. 最终路径轨道允许在唯一的 P2-P02 LightGBM 输出后，应用无标签、确定性、参数冻结、测试期可复现的整井路径算子；
3. 学习型融合不属于确定性后处理，必须另走 outer/inner 严格嵌套 OOF；
4. 原始模型成绩、确定性管线成绩、影子成绩和榜单成绩必须分开登记。

三阶段旧实验只保留为档案；当前路线以 `docs/P3_FINAL_CANDIDATE影子验证实验卡.md` 和 `docs/三阶段突破路线与实验执行说明.md` 的最新路线更新为准。

## 2. 冻结基线

- 基线编号：`P3B00_group5_p2p02_v1`
- 来源：`P2_P02_multiscale_pf_paths_v1`
- 主 CV：`balanced_well_5fold_v1`
- 验证单位：完整井
- 井数：773
- 自然隐藏评价行：3,783,989
- 模型：单个 `lightgbm.LGBMRegressor`
- 特征数：41
- 树数：1,734
- 随机种子：29
- micro RMSE：`10.305704992073148`
- 五折 RMSE：
  - fold 0：`10.170796687354473`
  - fold 1：`9.4624641719035`
  - fold 2：`9.181973787681697`
  - fold 3：`10.8754729397572`
  - fold 4：`11.639196676252652`

正式比较必须使用相同 fold、相同评价行、相同模型参数和相同评分实现。

## 3. 影子集规则

三阶段固定影子集为 `P3_shadow_holdout_v1`：

- 共 116 口井；
- 各折井数为 24、23、23、23、23；
- 剩余开发集为 657 口井；
- 选择时只使用井号、fold、隐藏段长度、隐藏段 GR 观测率、XY、方位角和 Typewell 指纹等无标签信息；
- 没有读取影子井的隐藏 TVT、残差、误差、RMSE、surface 或 oracle 信息。

用户已在 2026-07-20 明确授权一次性打开影子集，只验证预先冻结的 `P4_FINAL00_UP03_v1`：

- 打开前必须同时预登记标准影子评分和严格影子评分；
- 只比较 P3B00 与冻结 UP03，不得同时评价 UP01、UP02、UP08 或多个比例；
- 合法候选必须先落盘并校验哈希，之后才能读取影子目标；
- 打开后不得用影子集选择特征、参数、阈值、路线或后处理；
- 不得因为结果不理想更换影子井，也不得把影子井重新当开发集。

## 4. 三类实验必须分开

### A 轨：特征研究

- 单个 LightGBM；固定原 41 列、1,734 棵树、seed 29、target 和 CV；
- 不做后处理或输出融合；每次只增加一个命名特征组；
- 结果标记为 `feature_ablation_cv`，不得覆盖 P3B00 artifact。

### B 轨：最终确定性路径

- 只允许一个学习模型：P2-P02 LightGBM；
- 其输出后可应用无标签、确定性、整井级、固定参数的 U 投影、平滑、固定滞后 PFS 和固定全局比例修正；
- OOF 与测试必须使用同一代码，保存修正前后的完整预测；
- 结果标记为 `deterministic_pipeline_development` 或 `shadow_confirmed_pipeline`。

### C 轨：学习型融合

任何 Ridge、第二个 LightGBM、候选选择器、每井权重或 stacking 都必须使用 outer/inner 严格嵌套 OOF。禁止用现成全局 OOF 预测直接训练外层融合器；结果标记为 `nested_fusion_cv`。

## 5. 当前执行顺序

```text
P3B00                         原始单模基线 10.305705                 已完成
P3_UP03                       657井确定性管线 9.728976               已独立确认
P3_UP08                       严格outer0仅改善0.065763              已停止
P4_FINAL00_UP03_v1            唯一冻结影子候选                       当前项
标准影子评分                  现有合法OOF → 冻结UP03                 已预登记
严格影子评分                  每折只用非影子且非本折开发井重训       已预登记
P4_FINAL_PIPELINE_UP03_v1      影子通过后生成测试提交管线             待定
```

不得在影子集上搜索其他路径或参数。只有标准与严格影子合同完成后，才根据预登记规则决定提交管线或关闭 UP03。

## 6. 统一实验流程

```text
读取 current_state.json
→ 先确定属于特征研究、确定性管线还是学习型融合
→ 写实验卡并冻结唯一假设
→ 运行 1～3 口井 smoke
→ 按对应轨道的预注册验证执行
→ 保存原始预测、最终预测、配置、哈希、指标和结论
→ 更新 registry.jsonl 与 current_state.json
```

普通实验不重复请求确认。以下情况必须停下：

- 需要改变冻结基线、主 CV、模型或评价区；
- 需要打开影子集且没有用户明确授权或没有预登记唯一候选；
- 需要测试时不可获得的信息；
- 数据缺失或歧义无法安全判断；
- 用户明确要求停止。

## 7. 晋级门槛

正式模型或特征实验相对当前冻结基线比较。

### folds 0～1 筛查门槛

- 合并 micro RMSE 至少改善 `0.20 ft`；
- 两折中任一折不得恶化超过 `0.10 ft`。

### 开发集完整五折门槛

- micro RMSE 至少改善 `0.10 ft`；
- 至少 4/5 折改善；
- folds 2～4 至少 2/3 折改善；
- 最差一折恶化不得超过 `0.25 ft`；
- 逐井配对 bootstrap 的 95% CI 上界小于 0；
- 胜井率至少 55%；
- P90 well RMSE 恶化不超过 `0.20 ft`；
- 收益来自最强 5% 井的比例不得超过 60%。

诊断实验不按模型分数晋级，而按实验卡中预先写明的可辨识性指标决定下一步。负结果只能否定当前实现，不能擅自否定整个信息源。

### 一次性影子管线门槛

- pooled micro RMSE 改善至少 `0.15 ft`；
- 至少 `3/5` 个影子子折改善；
- 井级胜率至少 `52%`；
- 井级 paired bootstrap 的 95% 区间上界严格小于 0；
- P90 well RMSE 恶化不超过 `0.20 ft`。

标准影子评分和严格影子评分必须在打开前一起预登记。影子结果不得用于重新选择比例或候选。

## 8. 数据、标签和缓存边界

- 自然隐藏区固定为 `TVT_input.isna()`；
- 所有 outer-train 拟合量必须在每个 fold 内重算；
- 验证井不得参与邻井统计、曲面、标准化、阈值或模型选择；
- oracle 输出必须与正式特征和正式缓存物理分开；
- 缓存指纹至少包含 fold、井列表、特征版本、PF 参数、模型参数和代码版本；
- 读取缓存时必须验证配置完全匹配；
- 禁止在不知道来源时复用旧缓存。

## 9. 运行、产物和 Git

长任务开始前必须说明井数、主要耗时、输出目录和中断恢复方式。运行中至少在 smoke、每折结束、门槛判断和异常时报告进度。

每个实验保存到：

```text
rogii_clean/artifacts/<experiment_id>/
```

至少保存配置、指标、逐井结果、逐折结果、运行时间和结论。正式模型实验还必须保存完整特征列表、参数、OOF 预测和泄漏审计。

当前状态写入 `rogii_clean/experiments/current_state.json`，所有正负结果追加到 `rogii_clean/experiments/registry.jsonl`。

Git、提交、推送和 PR 由用户操作。Codex 不创建 worktree，不因 Git 中断实验；阶段节点只给出用户可执行的命令。

## 10. 结论写法

每项实验必须明确区分：

```text
数据直接证明的事实：
基于事实的合理推断：
仍然没有验证的猜测：
当前实验只能否定的具体实现：
下一步最便宜的验证：
```

所有说明尽量使用清晰中文。不要用术语代替证据，也不要把 oracle 上限写成可提交成绩。

每项结果还必须填写唯一 `result_class`：

```text
feature_ablation_cv
deterministic_pipeline_development
nested_fusion_cv
shadow_confirmed_pipeline
```

报告始终分别显示原始 LightGBM、确定性最终管线、影子验证和榜单成绩，禁止混写成一个 CV。

## 11. Phase 4 当前冻结状态（2026-07-20）

一次性影子确认已经完成，不再处于“待打开”状态：

```text
P4_FINAL00_UP03_v1
标准影子：10.492112 → 10.097720，改善 0.394391，5/5 折改善，通过
严格影子：10.956707 → 10.353233，改善 0.603474，5/5 折改善，通过
最终判定：standard_gate AND strict_gate = true
result_class：shadow_confirmed_pipeline
```

正式冻结提交管线为 `P4_FINAL_PIPELINE_UP03_v1`。影子集 `P3_shadow_holdout_v1` 已经消耗并永久锁定；后续实验不得读取它来选择特征、参数、比例、门控或路线，也不得把它并回开发集。

后续研究仍以达到正式评价 `<9` 为目标，只能使用 657 口开发井进行路线选择。新确定性整井算子先登记为 `deterministic_pipeline_development`；学习型权重或融合必须走 `nested_fusion_cv`。除非用户另行要求，不能再次运行任何影子候选比较。
