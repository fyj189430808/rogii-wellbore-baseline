# ROGII 项目协作规则（AGENTS.md）

## 0. 当前阶段的唯一目标

当前阶段不是继续堆模型、追排行榜分数或同时探索很多新方向。

当前阶段只有一个最高优先级：

> 让我真正看懂并能够独立复现当前 PF 10.7 基线，包括数据如何进入、用了哪些特征、模型如何运行、每个参数控制什么、预测如何生成、CV 如何评分。

在我完成理解之前，不得主动开启新的大实验，不得继续叠加新的模型，不得为了“可能提高分数”而扩大项目复杂度。

旧项目只作为实验档案保留。新的干净目录只迁移经过我亲自理解和验证的代码。

---

## 1. 人类理解高于代码速度

Codex 的职责是帮助我理解、实现和验证，而不是代替我做所有研究判断。

所有工作必须遵守以下顺序：

1. 先解释问题。
2. 再说明输入和输出。
3. 再画清楚数据流。
4. 再解释算法原理。
5. 再列出模型、特征和参数。
6. 得到我理解或允许后，再写代码或修改代码。
7. 代码完成后，逐段解释代码实际做了什么。
8. 最后才运行实验和解释结果。

不能先写一大堆复杂代码，再用几句话概括。

不能因为代码可以运行，就认为任务已经完成。

“完成”的标准是：我能够用自己的话解释这段代码的作用、输入、输出和关键算法。

---

## 2. 当前 PF 10.7 基线必须先完成代码考古

在进行任何新建模实验前，必须先完成当前 PF 10.7 基线的完整导读。

必须输出以下文档：

```text
docs/
├── 00_project_map.md              # 项目文件地图：每个文件是干什么的
├── 01_pf_entrypoint.md            # PF 从哪个文件、哪个函数开始运行
├── 02_pf_data_flow.md             # 数据从 CSV 到最终预测的完整流程
├── 03_pf_algorithm.md             # 粒子滤波算法的通俗解释和代码对应关系
├── 04_feature_dictionary.md       # 所有特征的名字、公式、来源和作用
├── 05_parameter_dictionary.md     # 所有参数的名字、默认值、单位和影响
├── 06_model_inventory.md          # 项目使用的全部模型和每个模型的职责
├── 07_validation_and_metrics.md   # CV、fold、评价行和 RMSE 的实现
└── 08_reproduction_steps.md       # 从零复现 PF 10.7 的命令和步骤
```

这些文档没有完成前，不得声称已经“理解 PF”。

### 2.1 项目文件地图必须回答

对每个相关文件说明：

- 文件路径；
- 文件用途；
- 是否属于 PF 10.7 主流程；
- 被谁调用；
- 调用了谁；
- 输入文件；
- 输出文件；
- 是否可以暂时忽略。

### 2.2 PF 数据流必须回答

必须从头到尾说明：

```text
horizontal_well.csv
typewell.csv
训练/验证 fold
可见前缀与隐藏段
GR 与 TVT 数据
粒子初始化
粒子状态更新
观测似然
重采样
路径平滑
多随机种子结果
最终加权
carry hold 收缩
最终 TVT 预测
RMSE 评分
```

每一步都必须指出对应代码位置。

### 2.3 特征字典必须完整

每一个特征都必须记录：

| 字段 | 含义 |
|---|---|
| 特征名 | 代码中的准确名称 |
| 中文解释 | 这个特征代表什么 |
| 计算公式 | 如何从原始数据算出 |
| 输入来源 | 使用哪些列或模型输出 |
| 数据单位 | ft、无量纲、相关系数等 |
| 使用位置 | 哪个模型或函数使用 |
| 推理时是否合法 | 测试时是否能够获得 |
| 可能的作用 | 为什么它可能有用 |
| 潜在风险 | 泄漏、缺失、尺度或过拟合风险 |

不能只输出一个特征名列表。

### 2.4 参数字典必须完整

每一个关键参数都必须记录：

| 字段 | 含义 |
|---|---|
| 参数名 | 代码中的准确名称 |
| 当前值 | 当前 PF 10.7 使用的值 |
| 中文解释 | 控制什么 |
| 单位 | ft、概率、粒子数等 |
| 增大后的影响 | 路径会怎样变化 |
| 减小后的影响 | 路径会怎样变化 |
| 选择方式 | 手工、训练折选择或固定 |
| 是否影响缓存 | 是否需要重新计算 |

尤其必须解释：

- 粒子数；
- 随机种子数；
- 状态转移噪声；
- 速度或动量参数；
- GR 观测噪声；
- Typewell 搜索范围；
- 温度参数；
- 重采样阈值；
- 平滑参数；
- carry hold 收缩比例；
- 各种 scale 的含义。

---

## 3. 代码必须为学习而写，而不是为“专业感”而写

### 3.1 默认使用简单、直接的代码结构

优先：

```text
一个脚本完成一个明确任务
少量函数
清晰的执行顺序
明确的输入和输出
```

避免：

- 为了抽象而抽象；
- 过多类继承；
- 工厂模式；
- 注册器模式；
- 依赖注入；
- 元编程；
- 装饰器堆叠；
- 多层配置覆盖；
- 隐式全局状态；
- 一行完成很多操作；
- 大量难以追踪的工具函数；
- 为未来可能需求提前设计复杂框架。

只有当简单实现已经无法维护时，才能提出更复杂结构，并且必须先解释为什么。

### 3.2 新代码的中文注释要求

当前学习阶段，新写或重写的核心代码必须做到：

- 除空行、单独括号和非常明显的语法行外，每一行都要有中文解释。
- 行尾适合写注释时，使用行尾中文注释。
- 行尾不适合时，在上一行写中文注释。
- 每个变量第一次出现时，必须说明它代表什么、形状是什么、单位是什么。
- 每个函数上方必须有中文说明，写清功能、输入、输出和副作用。
- 每个复杂公式必须拆开写，不能压成一行。
- 每个数组变换必须说明变换前后的 shape。
- 每个模型调用必须说明输入特征和输出含义。
- 每个参数必须说明为什么使用当前值。
- 不允许只写“读取数据”“训练模型”这种没有信息量的注释。
- 注释必须解释“为什么”，不能只是把英文代码翻译成中文。

推荐格式：

```python
# 读取当前水平井的数据，每一行代表一个 MD 采样点
horizontal_df = pd.read_csv(horizontal_path)

# 取出测量深度，形状为 [采样点数量]，单位为 ft
md = horizontal_df["MD"].to_numpy(dtype=np.float64)

# 计算结构坐标 U，U 表示地层面高程，公式为 U = TVT + Z
u_visible = tvt_visible + z_visible
```

不推荐：

```python
df = pd.read_csv(path)  # 读取
x = df["X"].values      # 获取X
```

### 3.3 禁止难以阅读的写法

除非我明确允许，否则不要使用：

- 超过一层的列表推导式；
- 复杂 lambda；
- 连续链式调用；
- 一行多个赋值；
- 隐晦缩写变量；
- 没有类型和含义说明的 `x1`、`tmp`、`res`、`obj`；
- `*args` 和 `**kwargs` 隐藏参数；
- 魔法数字；
- 没有解释的广播操作；
- 过度向量化导致代码无法理解。

宁可多写几行，也要让我能逐行跟踪。

### 3.4 变量命名

变量名必须表达真实含义。

推荐：

```python
visible_tvt
hidden_start_index
particle_tvt_positions
typewell_gr_values
observation_log_likelihood
well_level_rmse
```

不推荐：

```python
a
b
x
xx
tmp
arr2
val
pred2
```

数学公式中的临时变量可以使用短名称，但必须紧邻公式解释。

---

## 4. 修改旧代码前必须先解释

修改任何旧代码前，必须先输出一张“修改说明卡”。

```text
修改目标：
当前代码行为：
准备修改的文件：
准备修改的函数：
为什么要修改：
修改前的数据流：
修改后的数据流：
会影响哪些特征：
会影响哪些模型：
会影响哪些参数：
会影响哪些缓存：
预期结果：
可能风险：
如何回滚：
```

没有这张说明卡，不得开始大规模改动。

每次修改应尽量只改变一个明确因素。

不能同时：

- 改特征；
- 改模型；
- 改 fold；
- 改评分；
- 改后处理。

否则无法判断结果来自哪里。

---

## 5. 禁止静默增加、删除或替换模型

任何模型、特征、后处理或参数发生变化，都必须明确记录。

每次实验前必须列出：

```text
本次使用的模型：
本次使用的特征：
本次使用的参数：
与当前基线唯一不同的地方：
```

每次实验后必须列出：

```text
实际运行的模型：
实际使用的特征数量：
完整特征列表保存位置：
实际参数保存位置：
实际预测保存位置：
与基线相比改变了什么：
```

不得出现以下情况：

- 报告说使用 PF，实际又混入了 GBDT；
- 报告说单模型，实际使用多个 OOF 输出；
- 删除一个特征但不说明；
- 参数由程序自动选择但报告中没有记录；
- 运行时读取了旧缓存，却不知道缓存来自哪个配置；
- 代码中存在多个版本，无法确定最终执行的是哪一个。

---

## 6. 新干净项目的结构

建议新目录使用简单结构：

```text
rogii_clean/
├── AGENTS.md                      # 当前协作规则
├── README.md                      # 项目总体说明
├── requirements.txt              # 明确依赖版本
├── configs/
│   └── pf_baseline.json           # PF 10.7 的唯一基线配置
├── data/
│   └── README.md                  # 数据路径和字段说明，不复制大文件
├── docs/                          # 人类可读的代码导读
├── scripts/
│   ├── inspect_data.py            # 查看数据结构
│   ├── reproduce_carry.py         # 复现末值延续
│   ├── reproduce_pf.py            # 复现 PF 10.7
│   └── score_predictions.py       # 统一评分
├── src/
│   ├── data_loading.py            # 数据读取
│   ├── fold_split.py              # 固定折分
│   ├── metrics.py                 # 统一指标
│   ├── particle_filter.py         # PF 核心算法
│   └── pf_features.py             # PF 使用的特征
├── experiments/
│   └── registry.jsonl             # 实验登记
├── artifacts/                     # 预测、指标、图表和配置
└── tests/                         # 关键逻辑测试
```

不要一开始建立几十层目录。

只有当文件确实过长并且我已经理解后，才允许进一步拆分。

---

## 7. PF 10.7 复现是进入新研究的门槛

新项目必须先一键复现以下结果：

- carry-forward 基线；
- 固定 fold 0 PF；
- 完整五折 PF；
- 相同评价行数；
- 相同井数量；
- 相同 fold；
- 相同 micro RMSE；
- 相同 macro RMSE；
- 相同 P90；
- 相同最差井；
- 相同预测文件。

允许存在的数值误差必须提前说明。

推荐命令：

```bash
python scripts/reproduce_pf.py --fold 0 --config configs/pf_baseline.json
```

运行时必须打印：

```text
当前 fold：
训练井数量：
验证井数量：
评价行数量：
粒子数：
随机种子数：
主要参数：
缓存是否命中：
当前处理到第几口井：
最终指标：
输出文件：
```

在我能够解释复现流程前，不得继续新的金牌实验。

---

## 8. 回答问题时必须说人话

所有解释使用清晰、普通的中文。

当出现专业术语时，必须按以下格式解释：

```text
先用一句人话解释。
再给出专业名称。
最后指出它在当前代码中的位置和作用。
```

例如：

```text
重采样就是淘汰权重很低的粒子，并复制权重高的粒子。
它的专业名称是 particle resampling。
在当前 PF 中，它用于让计算资源集中到更可能的 TVT 路径附近。
```

每次解释算法时必须包含：

- 它解决什么问题；
- 输入是什么；
- 输出是什么；
- 核心步骤；
- 为什么可能有效；
- 它会在什么情况下失败；
- 当前项目的代码位置；
- 一个小型数值例子或直观比喻。

不要用新的术语解释旧术语。

---

## 9. 任何新实验都必须先写实验卡

```text
实验编号：
实验名称：
唯一假设：
为什么值得验证：
与当前最佳基线唯一不同之处：
合法输入：
预测目标：
固定 fold：
固定评价行：
正对照：
负对照：
oracle 诊断：
成功门槛：
停止条件：
预计运行时间：
需要生成的文件：
失败后能否否定整个方向：
失败后只能否定哪一种实现：
```

必须特别区分：

### 9.1 实现失败

当前代码、参数或表示不合理。

实现失败不能否定整个方向。

### 9.2 当前假设失败

例如“所有井共享一张只由 X/Y 决定的单值地层面”被数据直接反驳。

### 9.3 信息源没有价值

只有真实信息或 oracle 也无法改善时，才能得出这种强结论。

不能把一次具体模型失败写成“整个 GR 方向失败”“整个地层方向失败”或“邻井没有用”。

---

## 10. 验证和评分规则

### 10.1 固定验证单位

- 以完整井为验证单位。
- 不允许随机拆分数据行。
- 同一口井的任何行不能同时出现在训练和验证。
- 主 CV 必须保持当前固定 spatial pad 分组。
- 比较实验必须使用相同 fold、相同评价行和相同指标实现。

### 10.2 固定基线

每个实验至少比较：

- carry-forward；
- 当前合法 PF；
- 本次新方法。

不能为了让新方法显得更好而更换验证井或评价行。

### 10.3 必须报告的指标

- micro RMSE；
- macro per-well RMSE；
- 每折 RMSE；
- 中位井 RMSE；
- P90 well RMSE；
- 最差井 RMSE；
- 胜井率；
- 运行时间；
- 关键失败切片。

小提升还必须报告：

- 各折是否一致；
- 逐井配对重采样；
- 改善置信区间。

---

## 11. 防止数据泄漏

以下信息若来自训练标签，必须在每个 outer fold 内重新计算：

- TVT 派生特征；
- 六个地层界面；
- 邻井标签；
- 空间曲面；
- OOF 预测；
- 拟合的标准化参数；
- 模型选择结果；
- 超参数选择结果。

验证井和验证 spatial pad 不得参与：

- 特征拟合；
- 邻井标签构造；
- 地层面构造；
- 缓存生成；
- 参数选择；
- 置信度阈值选择；
- 后处理选择。

自然评价区定义固定为：

```python
visible_mask = horizontal_df["TVT_input"].notna()  # 可见前缀
hidden_mask = horizontal_df["TVT_input"].isna()    # 隐藏评价段
```

隐藏 TVT 只能用于最终评分和明确标注的 oracle 诊断。

---

## 12. 缓存和复现

任何缓存必须包含完整配置指纹。

缓存键至少包含：

- fold；
- 井列表；
- 粒子数；
- 随机种子；
- GR 处理方式；
- Typewell 处理方式；
- 状态转移参数；
- 平滑参数；
- scale；
- carry hold；
- 特征版本；
- 代码版本或提交哈希。

读取缓存时必须打印：

```text
缓存文件：
缓存创建时间：
缓存配置：
当前配置：
是否完全匹配：
```

不能在不知道来源的情况下复用旧缓存。

每次实验必须保存：

```text
artifacts/<experiment_id>/
├── config.json
├── metrics.json
├── per_well.csv
├── predictions.parquet
├── feature_list.json
├── parameter_list.json
├── runtime.json
└── conclusion.md
```

正结果和负结果都要记录到：

```text
experiments/registry.jsonl
```

---

## 13. 长任务的运行规则

长任务必须：

- 先运行 1～3 口井的小样本；
- 再运行固定 fold 0；
- 通过门槛后才运行完整五折；
- 每口井或每个批次打印进度；
- 支持中断后继续；
- 保存中间结果；
- 不得无提示运行数小时。

开始运行前必须告诉我：

```text
将运行什么：
使用多少口井：
预计主要耗时来自哪里：
会生成哪些文件：
停止后如何继续：
```

不得只说“正在训练”。

---

## 14. 当前研究优先级

在新项目完成 PF 10.7 的逐行理解和复现前：

### 允许做

- 阅读旧代码；
- 整理文件调用关系；
- 解释 PF；
- 解释特征；
- 解释参数；
- 写小型演示代码；
- 添加中文注释；
- 添加单元测试；
- 复现 carry-forward；
- 复现 PF；
- 比较新旧预测是否完全一致。

### 暂不允许做

- 新增复杂模型；
- 大规模参数搜索；
- 新的模型融合；
- 新的候选排序器；
- 新的 Geology 模型；
- 新的邻井系统；
- 新的神经网络；
- 为追分而同时修改多个模块；
- 自动重构整个项目。

等我能够独立解释 PF 主流程后，再由我决定下一条研究主线。

---

## 15. Codex 不得替我做最终研究判断

Codex 可以：

- 提出假设；
- 写实验；
- 检查代码；
- 发现 bug；
- 生成图表；
- 汇总证据；
- 指出可能解释。

Codex 不可以：

- 因一次负实验擅自宣布整个方向死亡；
- 因一次正实验擅自宣布找到金牌方案；
- 在我不理解时继续扩大系统；
- 用专业术语掩盖不确定性；
- 把推测写成事实；
- 偷偷改变 CV、特征或模型；
- 为了得到更好分数而放松防泄漏规则。

结论必须分成：

```text
数据直接证明的事实：
基于事实的合理推断：
仍然没有验证的猜测：
当前实验只能否定的具体实现：
下一步最便宜的验证：
```

---

## 16. 每次提交代码后的解释模板

每次新增或修改代码后，必须回答：

```text
1. 这段代码解决什么问题？
2. 从哪个文件开始运行？
3. 输入文件有哪些？
4. 主要变量分别是什么？
5. 数据 shape 如何变化？
6. 调用了哪些函数？
7. 使用了哪些特征？
8. 使用了哪些模型？
9. 使用了哪些参数？
10. 最终输出到哪里？
11. 哪些地方最容易出错？
12. 我应该先阅读哪 20 行代码？
13. 如何用一个小样本验证它？
```

如果我说“看不懂”，必须降低解释难度，不能只重复原话。

---

## 17. 当前完成标准

只有满足以下条件，当前 PF 理解阶段才算完成：

- 我能说清 PF 的输入和输出；
- 我能解释一个粒子代表什么；
- 我能解释粒子如何移动；
- 我能解释 GR 似然如何计算；
- 我能解释为什么需要重采样；
- 我能解释 24 个随机种子产生什么；
- 我能解释 scale 和 carry hold；
- 我能列出 PF 使用的全部合法输入；
- 我能找到每个关键参数所在代码；
- 我能独立运行 fold 0；
- 我能读懂运行日志；
- 我能找到预测文件；
- 我能复算 micro RMSE；
- 新项目能够稳定复现 PF 基线；
- README 和 8 份 PF 导读文档已经完成。

在此之前，代码简洁和理解优先于任何排行榜提升。
