# ROGII 项目协作规则（三阶段路径突破版）

## 1. 当前阶段目标

三阶段的目标不是更换模型，而是突破当前整段路径表示的上限：

1. 改进 PF 整段路径的生成方式；
2. 让不同井、不同位置能够使用不同的路径可靠性权重；
3. 在严格 OOF 条件下预测低维、平滑的残差；
4. 保持单模型 LightGBM、固定 CV 和自然隐藏评价区不变。

三阶段完整研究依据以 `docs/三阶段突破路线与实验执行说明.md` 为准。旧阶段只保留为实验档案，不得与三阶段结果混用。

## 2. 冻结基线

- 基线编号：`P3B00_group5_p2p02_v1`
- 来源：`P2_P02_multiscale_pf_paths_v1`
- 主 CV：`balanced_well_5fold_v1`
- 验证单位：完整井
- 井数：773
- 自然隐藏评价行：3,783,989
- 模型：单个 `lightgbm.LGBMRegressor`
- 特征数：41
- 树数：1,734
- 随机种子：29
- micro RMSE：`10.305704992073148`
- 五折 RMSE：
  - fold 0：`10.170796687354473`
  - fold 1：`9.4624641719035`
  - fold 2：`9.181973787681697`
  - fold 3：`10.8754729397572`
  - fold 4：`11.639196676252652`

正式比较必须使用相同 fold、相同评价行、相同模型参数和相同评分实现。

## 3. 影子集规则

三阶段固定影子集为 `P3_shadow_holdout_v1`：

- 共 116 口井；
- 各折井数为 24、23、23、23、23；
- 剩余开发集为 657 口井；
- 选择时只使用井号、fold、隐藏段长度、隐藏段 GR 观测率、XY、方位角和 Typewell 指纹等无标签信息；
- 没有读取影子井的隐藏 TVT、残差、误差、RMSE、surface 或 oracle 信息。

在用户明确决定打开影子集之前：

- 不得用影子集选择特征、参数、阈值、路线或后处理；
- 不得查看影子集目标、误差或排名；
- 不得因为开发集结果不理想而更换影子井；
- 所有日常诊断和模型开发只能使用 657 口开发井。

## 4. 两类正式实验必须分开

### A 类：确定性路径实验

输入只来自当前井可见数据、Typewell、冻结 PF 路径及其确定性变换。此类实验可以直接作为 LightGBM 特征，但每次只能改变一个清楚命名的特征组。

### B 类：学习型残差实验

凡是使用训练标签学习残差系数、路径修正、可靠性权重或邻井先验，都必须在每个 outer fold 内重新拟合。验证井及其标签不得参与训练、参数选择、标准化或缓存生成。

不得把全量拟合的残差、oracle 系数或验证井真值派生量作为正式特征。

## 5. 当前执行顺序

三阶段按以下顺序推进：

```text
P3B00  冻结 10.3057 基线                       已完成
P3-CV00 建立并冻结 116 口无标签影子集          已完成
P3-D00 诊断 10.3 残差是否可由低维平滑函数表示  已完成
P3-D01 审计 PF 观测证据与固定温度权重           已完成
P3-PF01 缺失感知、稳健 GR 标定的多随机种子 PF  当前项
P3-PF02 固定有效路径数（目标 ESS）的整井加权
P3-PF03 分段似然、沿井深动态加权
P3-R01 严格 OOF 的低维平滑残差学习
```

只有新证据足以改变顺序时，才能先更新路线图并说明原因。不得同时修改路径、模型、CV 和后处理。

## 6. 统一实验流程

```text
读取 current_state.json
→ 写实验卡并冻结唯一假设
→ 运行 1～3 口井 smoke
→ 在开发集 folds 0～1 筛查
→ 按预注册门槛停止或继续
→ 通过后运行开发集完整五折
→ 保存产物和结论
→ 更新 registry.jsonl 与 current_state.json
→ 进入路线图下一项
```

普通实验不重复请求确认。以下情况必须停下：

- 需要改变冻结基线、主 CV、模型或评价区；
- 需要打开影子集；
- 需要测试时不可获得的信息；
- 数据缺失或歧义无法安全判断；
- 用户明确要求停止。

## 7. 晋级门槛

正式模型或特征实验相对当前冻结基线比较。

### folds 0～1 筛查门槛

- 合并 micro RMSE 至少改善 `0.20 ft`；
- 两折中任一折不得恶化超过 `0.10 ft`。

### 开发集完整五折门槛

- micro RMSE 至少改善 `0.10 ft`；
- 至少 4/5 折改善；
- folds 2～4 至少 2/3 折改善；
- 最差一折恶化不得超过 `0.25 ft`；
- 逐井配对 bootstrap 的 95% CI 上界小于 0；
- 胜井率至少 55%；
- P90 well RMSE 恶化不超过 `0.20 ft`；
- 收益来自最强 5% 井的比例不得超过 60%。

诊断实验不按模型分数晋级，而按实验卡中预先写明的可辨识性指标决定下一步。负结果只能否定当前实现，不能擅自否定整个信息源。

## 8. 数据、标签和缓存边界

- 自然隐藏区固定为 `TVT_input.isna()`；
- 所有 outer-train 拟合量必须在每个 fold 内重算；
- 验证井不得参与邻井统计、曲面、标准化、阈值或模型选择；
- oracle 输出必须与正式特征和正式缓存物理分开；
- 缓存指纹至少包含 fold、井列表、特征版本、PF 参数、模型参数和代码版本；
- 读取缓存时必须验证配置完全匹配；
- 禁止在不知道来源时复用旧缓存。

## 9. 运行、产物和 Git

长任务开始前必须说明井数、主要耗时、输出目录和中断恢复方式。运行中至少在 smoke、每折结束、门槛判断和异常时报告进度。

每个实验保存到：

```text
rogii_clean/artifacts/<experiment_id>/
```

至少保存配置、指标、逐井结果、逐折结果、运行时间和结论。正式模型实验还必须保存完整特征列表、参数、OOF 预测和泄漏审计。

当前状态写入 `rogii_clean/experiments/current_state.json`，所有正负结果追加到 `rogii_clean/experiments/registry.jsonl`。

Git、提交、推送和 PR 由用户操作。Codex 不创建 worktree，不因 Git 中断实验；阶段节点只给出用户可执行的命令。

## 10. 结论写法

每项实验必须明确区分：

```text
数据直接证明的事实：
基于事实的合理推断：
仍然没有验证的猜测：
当前实验只能否定的具体实现：
下一步最便宜的验证：
```

所有说明尽量使用清晰中文。不要用术语代替证据，也不要把 oracle 上限写成可提交成绩。
